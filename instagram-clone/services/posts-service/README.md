# Posts Service

This service will own content creation, post retrieval, media references, and post-centric data access.

## Expected API Surface

- `POST /posts`
- `GET /posts/:id`
- `GET /posts/feed`
- `DELETE /posts/:id`

This is a rough draft and will be refined as requirements firm up.
